// Pi. This is the ratio of the circumference of a circle to its diameter.

//sorting the float value
/* single line */
console.log(Number.parseFloat(Math.PI.toFixed(3)));

/* Multi line */
let pi = Math.PI;
let fix = pi.toFixed(3);
let clg = Number.parseFloat(fix);
console.log(clg);
