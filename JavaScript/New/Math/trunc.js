/* Returns the integral part of the a numeric expression, x, removing any fractional digits.
 If x is already an integer, the result is x. */
let value = 10.66;
let clg = Math.trunc(value);
console.log(clg);
