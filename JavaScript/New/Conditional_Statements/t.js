let player = 3 ;
let computer = Math.ceil(Math.random() * 3);
let P_win = "You Win";
let C_win = "Computer Win";
let tie = "Tie";

switch(player){
    case 1 :
        if(computer === 1 ){
            console.log(`${tie}`)
        }
        if(computer === 2 ){
            console.log(`${tie}`)
        }
        if(computer === 3 ){
            console.log(`${tie}`)
        }
        break;

}
